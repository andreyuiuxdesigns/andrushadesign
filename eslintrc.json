import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Andrey Yanovsky — Junior UI/UX Designer",
  description:
    "Portfolio of Andrey Yanovsky — Junior UI/UX and Product Designer based in Ireland. Open to remote work and freelance projects.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
